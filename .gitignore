/target
key.txt*
magi.toml
*.tmp

# Local encrypted conversation/knowledge DB (runtime; per-machine, never commit)
.magi-rs-memory.db
.magi-rs-memory.db-shm
.magi-rs-memory.db-wal

# Local developer state (per CLAUDE.local.md §1 "Politica de tracking")
CLAUDE.md
CLAUDE.local.md
.claude/
.remember/

# SBTDD local-only files (sbtdd-init Step 6)
sbtdd/
planning/

# Internal development docs (audit, strategy, roadmap — not public)
dev-docs/

# Graphify knowledge-graph outputs + local hook patch script (per-machine, never commit)
graphify-out/
patch_graphify_hooks.py
