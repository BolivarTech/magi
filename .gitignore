/target
key.txt*
magi.toml
*.tmp

# Local encrypted conversation/knowledge DB (runtime; per-machine, never commit)
.magi-rs-memory.db
.magi-rs-memory.db-shm
.magi-rs-memory.db-wal

# Local developer state (per CLAUDE.local.md §1 "Politica de tracking")
CLAUDE.md
CLAUDE.local.md
.claude/
.remember/

# SBTDD local-only files (sbtdd-init Step 6)
sbtdd/
planning/
.superpowers/
# Verifiers whose ONLY subject is the gitignored plan: tracking a checker while its
# target stays local would leave a script in the repo that nobody else can run.
/scripts/plan_contract_check.py
/scripts/plan_symbol_sweep.py

# Internal development docs (audit, strategy, roadmap — not public)
dev-docs/

# Graphify knowledge-graph outputs + local hook patch script (per-machine, never commit)
graphify-out/
patch_graphify_hooks.py

# Dev-tooling scripts (per-developer, per howtos — not publishable; canonical copy in ~/BolivarTech/AI_Tools/)
/scripts/graphify_ollama.py
/scripts/claude_usage.py
/scripts/sleep_signal.py
__pycache__/
*.pyc

# Generated by scripts/claude_usage.py (per-developer usage snapshot)
/claude-usage.txt
/scripts/claude-usage*.txt

# Smoke harness: the real config carries the passphrase; the lock is runtime state
smoke/smoke.toml
smoke/.lock
